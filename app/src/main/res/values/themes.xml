<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base Application Theme -->
    <style name="Theme.CampusExpense" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary Colors -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <item name="colorPrimaryContainer">@color/primary_container</item>
        <item name="colorOnPrimaryContainer">@color/on_primary_container</item>

        <!-- Secondary Colors -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/on_secondary</item>
        <item name="colorSecondaryContainer">@color/secondary_container</item>
        <item name="colorOnSecondaryContainer">@color/on_secondary_container</item>

        <!-- Tertiary Colors -->
        <item name="colorTertiary">@color/tertiary</item>
        <item name="colorOnTertiary">@color/on_tertiary</item>
        <item name="colorTertiaryContainer">@color/tertiary_container</item>
        <item name="colorOnTertiaryContainer">@color/on_tertiary_container</item>

        <!-- Error Colors -->
        <item name="colorError">@color/error</item>
        <item name="colorOnError">@color/on_error</item>
        <item name="colorErrorContainer">@color/error_container</item>
        <item name="colorOnErrorContainer">@color/on_error_container</item>

        <!-- Background Colors -->
        <item name="android:colorBackground">@color/background</item>
        <item name="colorOnBackground">@color/on_background</item>
        <item name="colorSurface">@color/surface</item>
        <item name="colorOnSurface">@color/on_surface</item>
        <item name="colorSurfaceVariant">@color/surface_variant</item>
        <item name="colorOnSurfaceVariant">@color/on_surface_variant</item>

        <!-- Outline -->
        <item name="colorOutline">@color/outline</item>
        <item name="colorOutlineVariant">@color/outline_variant</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation Bar -->
        <item name="android:navigationBarColor">@color/surface</item>
        <item name="android:windowLightNavigationBar">true</item>

        <!-- Material Components -->
        <item name="materialButtonStyle">@style/Widget.CampusExpense.Button.Primary</item>
        <item name="textInputStyle">@style/Widget.CampusExpense.TextInputLayout</item>
        <item name="materialCardViewStyle">@style/Widget.CampusExpense.Card</item>

        <!-- Accessibility -->
        <item name="android:focusableInTouchMode">true</item>
    </style>

    <!-- Toolbar Theme -->
    <style name="ToolbarTheme" parent="ThemeOverlay.Material3.Dark">
        <item name="colorOnSurface">@color/on_primary</item>
        <item name="colorControlNormal">@color/on_primary</item>
        <item name="android:textColorPrimary">@color/on_primary</item>
    </style>

    <!-- High Contrast Theme (Accessibility) -->
    <style name="Theme.CampusExpense.HighContrast" parent="Theme.CampusExpense">
        <item name="colorPrimary">@color/high_contrast_primary</item>
        <item name="colorOnPrimary">@color/high_contrast_on_primary</item>
        <item name="colorSurface">@color/high_contrast_surface</item>
        <item name="colorOnSurface">@color/high_contrast_on_surface</item>
        <item name="colorOutline">@color/high_contrast_outline</item>

        <!-- Increase touch target sizes -->
        <item name="minTouchTargetSize">48dp</item>
        <item name="buttonStyle">@style/Widget.CampusExpense.Button.Primary</item>
    </style>

</resources>